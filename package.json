{
  "name": "kitmono",
  "version": "1.0.0",
  "scripts": {
    "format": "turbo run format",
    "lint": "turbo run lint",
    "build": "turbo run build"
  },
  "devDependencies": {
    "turbo": "^1.9.9"
  }
}